LOG를 ->
Session을 약간 따로 빼서 다른 라이브러리에서 작업하게 만들려고함
Session 자체로 풀링 시켜서 사용할 예정

Buffer 구현해야함 -> 문제는 여기서 발생하는 비용이 생각보다 큼 -> 즉 제로카피 버퍼를 만들어야할듯.
(대충 한번에 큰 메모리 할 당 받고 거기에 데이터 쓰면서 사용하게 해야할듯)

packet -> 만들면 ->버퍼에 쓰고 -> 버퍼에 쓴걸 다시 ->WSABuf에 옮겨서 보내야하는데





------------------------
//서버
서버 시작시 어떻게 작동 시 킬지 

제로 카피 버퍼는 중간 복사 작업이 없이 원본 메모리 버퍼에서 대상 
대상 메모리 버퍼로 데이터를 직접 전송하는 데이터를 전송하는 방법
이는 버퍼 간의 데이터 복사와 관련된 오버헤드를 줄이므로 데이터 전송 
성능을 향상시키는데 사용할 수 있다.


//서버구조를  
session -> packet 작성 
buffer에다 저장을 해버리면 -> 이게 다시 copy -> 
buffer 쓰고 다시 그 버퍼를 다시 쓰고 이 방법이라 
아니면 그냥 그 자체 버퍼 위치를 주는것도 나쁘지 않을듯

//API 서버 구조
로그인 하게 되면 sha-256으로 해쉬값 전달 
클라이언트에서는 이 값을 가지고 사용

근데 이렇게 만들면 문제가 있을듯 하다 -> 누군가 sha-256 값만 훔쳐가면
닶이 없다.
그래도 일단 이렇게 만들자 -> 
API 서버에서 body에 json 데이터를 넣고 싶으면 꼭 json으로 파싱해서 보내기 안 그럼 
string 값으로 다 변환 되어서 넘어감
아니면 해쉬 값을 하나 더 주는 방법





//클라
어떻게 설계 할것인가?   

c# 도 마찬가지로 c++과 같은 코딩 스타일을 따른다.